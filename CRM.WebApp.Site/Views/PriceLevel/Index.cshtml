@model IEnumerable<CRM.WebApp.Site.Models.PriceLevelViewModel>

<div class="col-md-8 ml-auto mr-auto">
    <h2 class="text-center">Níveis de Preço</h2>
    <p class="text-center">
        Nessa seção ficam armazenados todos os níveis de preço disponíveis.
    </p>
</div>
<a asp-action="Create" class="btn btn-primary">
    Adicionar nível de preço
</a>
<div class="row mt-5">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <div class="toolbar">
                    <!--        Here you can write extra buttons/actions for the toolbar              -->
                </div>

                <table id="datatable" class="table table-striped">
                    <thead>
                        <tr>
                            <th>Nome do Nível</th>
                            <th>Percentual de Desconto</th>
                            <th>Valor Base</th>
                            <th class="sorting_desc_disabled sorting_asc_disabled text-right">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var priceLevel in Model)
                        {
                            <tr>
                                <td>@priceLevel.LevelName</td>
                                <td>@priceLevel.DiscountPercentage</td>
                                <td>@priceLevel.ValueBase</td>
                                <td class="text-right">
                                    <a class="btn btn-link btn-warning btn-icon btn-sm edit" href="@Url.Action("Edit", new { id = priceLevel.PriceLevelID })"><i class="tim-icons icon-pencil"></i></a>
                                    <a class="btn btn-link btn-danger btn-icon btn-sm btn-neutral remove" href="@Url.Action("Delete", new { id = priceLevel.PriceLevelID })"><i class="tim-icons icon-simple-remove"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Nome do Nível</th>
                            <th>Percentual de Desconto</th>
                            <th>Valor Base</th>
                            <th class="disabled-sorting text-right">Ações</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>