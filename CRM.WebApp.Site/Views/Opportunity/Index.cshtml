@model IEnumerable<CRM.WebApp.Site.ViewModels.OpportunityViewModel>

<div class="col-md-8 ml-auto mr-auto">
    <h2 class="text-center">Oportunidades</h2>
    <p class="text-center">
        Nessa seção ficam armazenadas todas as oportunidades de venda de ingresso das olimpíadas.
    </p>
</div>
<a asp-action="Create" class="btn btn-primary">
    Adicionar oportunidade
</a>
<div class="row mt-5">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <div class="toolbar">
                    <!--        Here you can write extra buttons/actions for the toolbar              -->
                </div>

                <table id="datatable" class="table table-striped">
                    <thead>
                        <tr>
                            <th>Descrição</th>
                            <th>Valor Estimado</th>
                            <th>Data de Fechamento Esperada</th>
                            <th class="sorting_desc_disabled sorting_asc_disabled text-right">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var opportunity in Model)
                        {
                            <tr>
                                <td>@opportunity.Description</td>
                                <td>@opportunity.EstimatedValue</td>
                                <td>@opportunity.ExpectedCloseDate</td>
                                <td class="text-right">
                                    <a class="btn btn-link btn-warning btn-icon btn-sm edit" href="@Url.Action("Edit", new { id = opportunity.OpportunityID })"><i class="tim-icons icon-pencil"></i></a>
                                    <a class="btn btn-link btn-danger btn-icon btn-sm btn-neutral remove" href="@Url.Action("Delete", new { id = opportunity.OpportunityID })"><i class="tim-icons icon-simple-remove"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Descrição</th>
                            <th>Valor Estimado</th>
                            <th>Data de Fechamento Esperada</th>
                            <th class="disabled-sorting text-right">Ações</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>